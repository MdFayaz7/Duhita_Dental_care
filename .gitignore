# Environment files — NEVER commit these
.env
.env.local
.env.production
.env.*.local

# Dependencies
node_modules/

# macOS
.DS_Store

# Uploads (local disk files — not needed in git, GridFS handles this)
backend/uploads/*
!backend/uploads/.gitkeep

# Frontend build output
duhita-dental/dist/

# Logs
*.log
npm-debug.log*

# Local Claude Code config
.claude/

# Design source material (originals, not used by the build)
duhita-dental/duhita resources/
